---
ip: '127.0.0.1'
port: 9001
# std_dev_threshold: 1
std_dev_threshold: '' # give blank string to accept the second valid position sent
#
# MOOS
ros: '' # leave the unused comm system as an empty string
moos:
    ip: '192.168.1.44'
    port: 9000
    # delete data older than this if not filled, do not send latest if older than this
    time_buffer: 7.0 
    #
    sensor: 'gNovatel'
    desired_variables:
    - 'zX'
    - 'zY'
    - 'zZ'